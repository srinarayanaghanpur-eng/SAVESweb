{
  "name": "savesweb",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "smoke": "playwright test --config=playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0"
  }
}
